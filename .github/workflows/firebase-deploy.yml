name: Firebase Auto Deploy (Service Account Fix)

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - '**.txt'
      - '**.png'
      - '**.jpg'
      - '**.jpeg'
      - '**.gif'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔹 Checkout repository
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install Firebase CLI
        run: npm install -g firebase-tools

      - name: 🔑 Decode and Save Service Account JSON
        run: |
          echo '${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}' > ${HOME}/gcp-key.json
        shell: bash

      - name: 📂 Install Functions dependencies
        run: |
          cd functions
          npm ci
          cd ..

      - name: 🧪 Run lint check (optional)
        run: |
          cd functions
          npm run lint || echo "Lint warnings ignored"
          cd ..

      - name: 🚀 Deploy to Firebase (Hosting + Functions)
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ HOME }}/gcp-key.json
        run: |
          firebase deploy --only hosting,functions --non-interactive --project kids-worksheet-store
